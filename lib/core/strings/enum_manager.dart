enum CubitStatuses { init, loading, done, error }

enum ToScreen { non, confirmCode, policy, main }

enum UserType { family, adultOnly, kidsOnly }

enum PricingMatrixType { day, date }

enum FilterItem { activity, group, country, city }

enum NavItems { home, students, notifications, news }

enum UpdateType { name, phone, address, pass }

enum HomeCards {
  students,
  cashPayment,
  inventory,
  examTable,
  audit,
  debit,
  teachers,
  employees,
}
